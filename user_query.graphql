query UserQuery {
  viewer {
    login
  }
}

query OrganizationRepositories($login: String!) {
  organization(login: $login, after: $after) {
    repositories(first: 100) {
      totalCount
      nodes{
        name,
        nameWithOwner,
        sshUrl,
        url
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
}

query RepositoryDefaultBranch($owner: String!, $name: String!) {
  repository(owner: $owner, name: $name) {
    defaultBranchRef {
      name
    }
  }
}