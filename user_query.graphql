query UserQuery {
  viewer {
    login
  }
}

query OrganizationRepositories($login: String!) {
  organization(login: $login) {
    repositories(first: 100) {
      totalCount
      nodes{
        name,
        nameWithOwner,
        sshUrl,
        url
      }
    }
  }
}